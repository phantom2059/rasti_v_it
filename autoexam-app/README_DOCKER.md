# 🐳 AutoExam - Docker сборка

## ⚡ Быстрый старт

```bash
cd autoexam-app
docker-compose up --build
```

Приложение будет доступно на **http://localhost:8000**

## 📋 Требования

- **GPU**: NVIDIA с 12+ GB VRAM (рекомендуется T4 или лучше)
- **Docker** с поддержкой GPU (nvidia-container-toolkit)
- **CUDA** 11.8 или выше

## ✅ Проверка перед сборкой

```bash
# Проверка GPU
nvidia-smi

# Проверка Docker с GPU
docker run --rm --gpus all nvidia/cuda:11.8.0-base-ubuntu22.04 nvidia-smi
```

## 🔧 Сборка

### Вариант 1: Docker Compose (рекомендуется)
```bash
docker-compose up --build
```

### Вариант 2: Скрипт сборки
```bash
# Linux/Mac
./build.sh

# Windows
build.bat
```

### Вариант 3: Прямая сборка
```bash
docker build -f Dockerfile.gpu -t autoexam-app:latest .
```

## 🚀 Запуск

```bash
# В фоновом режиме
docker-compose up -d

# С логами
docker-compose up
```

## 📊 Мониторинг

```bash
# Логи контейнера
docker-compose logs -f

# Использование GPU
watch -n 1 nvidia-smi

# Статус контейнера
docker-compose ps
```

## 🛑 Остановка

```bash
docker-compose down
```

## 📝 Важные замечания

- ✅ **Все настроено на GPU по умолчанию**
- ✅ `docker-compose.yml` автоматически использует `Dockerfile.gpu`
- ⏱️ Первая сборка займет 30-60 минут
- 💾 Минимум 50 GB свободного места на диске

## 📚 Дополнительная документация

- `QUICKSTART.md` - быстрый старт
- `BUILD_INSTRUCTIONS.md` - подробная инструкция по сборке
- `CHECKLIST.md` - чеклист для проверки готовности
- `DOCKER.md` - полная документация по Docker

## 🐛 Решение проблем

### GPU не обнаружен
```bash
# Установите nvidia-container-toolkit
sudo apt-get install -y nvidia-container-toolkit
sudo systemctl restart docker
```

### Ошибка сборки
- Проверьте логи: `docker-compose logs`
- Убедитесь что все файлы на месте
- Проверьте свободное место на диске

### Медленная работа
- Убедитесь что GPU используется: `nvidia-smi`
- Проверьте что CUDA версия совместима

## 📞 Поддержка

При возникновении проблем смотрите `BUILD_INSTRUCTIONS.md`

