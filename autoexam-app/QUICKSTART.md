# 🚀 Быстрый старт - AutoExam Docker

## Требования

- **GPU**: NVIDIA с 12+ GB VRAM (рекомендуется T4 или лучше)
- **Docker** с поддержкой GPU (nvidia-container-toolkit)
- **CUDA** 11.8 или выше

## Проверка GPU

```bash
nvidia-smi
docker run --rm --gpus all nvidia/cuda:11.8.0-base-ubuntu22.04 nvidia-smi
```

## Сборка и запуск (один шаг)

```bash
cd autoexam-app
docker-compose up --build
```

Или используйте скрипт сборки:

**Linux/Mac:**
```bash
chmod +x build.sh
./build.sh
docker-compose up -d
```

**Windows:**
```cmd
build.bat
docker-compose up -d
```

## Доступ к приложению

После запуска откройте: **http://localhost:8000**

## Проверка работы

```bash
# Логи контейнера
docker-compose logs -f

# Использование GPU
watch -n 1 nvidia-smi

# Статус контейнера
docker-compose ps
```

## Остановка

```bash
docker-compose down
```

## Важно

- Первая сборка может занять 30-60 минут (загрузка зависимостей и моделей)
- Убедитесь что GPU доступен перед запуском
- Минимум 12 GB VRAM требуется для работы моделей

## Проблемы?

Смотрите подробную инструкцию в `BUILD_INSTRUCTIONS.md`

