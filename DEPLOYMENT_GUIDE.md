# Полный гайд по развертыванию AutoExam

## Содержание

1. [Требования к системе](#требования-к-системе)
2. [Подготовка окружения](#подготовка-окружения)
3. [Установка зависимостей](#установка-зависимостей)
4. [Настройка моделей](#настройка-моделей)
5. [Запуск сервиса](#запуск-сервиса)
6. [Развертывание в продакшене](#развертывание-в-продакшене)
7. [Мониторинг и логирование](#мониторинг-и-логирование)
8. [Решение проблем](#решение-проблем)

---

## Требования к системе

### Минимальные требования:

**Оборудование:**
- CPU: 4+ ядра
- RAM: 16 GB (рекомендуется 32 GB)
- GPU: NVIDIA GPU с поддержкой CUDA (минимум 8 GB VRAM, рекомендуется 16+ GB)
- Дисковое пространство: 50+ GB (для моделей и данных)

**Программное обеспечение:**
- **ОС**: Linux (Ubuntu 20.04+), Windows 10/11, или macOS
- **Python**: 3.10 или выше (рекомендуется 3.11)
- **Node.js**: 18.x или выше
- **npm**: 9.x или выше
- **CUDA**: 11.8 или 12.4+ (для GPU)
- **cuDNN**: совместимая версия с CUDA

### Проверка системы:

```bash
# Проверка Python
python --version  # или python3 --version

# Проверка Node.js
node --version

# Проверка npm
npm --version

# Проверка CUDA (если есть GPU)
nvidia-smi
```

---

## Подготовка окружения

### 1. Клонирование репозитория

```bash
git clone https://github.com/phantom2059/rasti_v_it.git
cd rasti_v_it/autoexam-app
```

### 2. Создание виртуального окружения Python

**Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Windows:**
```cmd
python -m venv venv
venv\Scripts\activate
```

### 3. Обновление pip и базовых инструментов

```bash
pip install --upgrade pip setuptools wheel
```

---

## Установка зависимостей

### 1. Установка PyTorch с CUDA

**Важно:** Сначала установите PyTorch с поддержкой CUDA:

**Для CUDA 12.4:**
```bash
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
```

**Для CUDA 11.8:**
```bash
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
```

**Для CPU-only (не рекомендуется для продакшена):**
```bash
pip install torch torchvision
```

### 2. Установка Python зависимостей

```bash
pip install -r requirements.txt
```

Это установит:
- `transformers` - библиотека для работы с моделями
- `accelerate` - ускорение инференса
- `fastapi` и `uvicorn` - веб-сервер
- `pandas` - обработка данных
- `scikit-learn` - вычисление схожести
- `peft` - для работы с адаптерами
- и другие зависимости

### 3. Установка Node.js зависимостей

```bash
npm install
```

Это установит:
- React и React Router
- Vite (сборщик)
- TailwindCSS (стили)
- Axios (HTTP клиент)
- Framer Motion (анимации)
- и другие зависимости

### 4. Проверка установки

```bash
# Проверка Python пакетов
pip list | grep torch
pip list | grep transformers

# Проверка Node.js пакетов
npm list --depth=0
```

---

## Настройка моделей

### 1. Структура моделей

Проект использует следующие модели:
- **Qwen2.5-VL-3B-Instruct** - для генерации подписей к изображениям
- **Fine-tuned Qwen** (адаптер `qwen_sft_exam/`) - для оценки ответов
- **ruBERT** - для вычисления семантической схожести

### 2. Проверка наличия адаптера

Убедитесь, что папка `qwen_sft_exam/` содержит:
- `adapter_model.safetensors` - веса адаптера
- `adapter_config.json` - конфигурация
- `tokenizer.json` и другие файлы токенизатора

**Если адаптер отсутствует:**
1. Скачайте адаптер с Hugging Face или из бэкапа
2. Поместите в папку `qwen_sft_exam/`
3. Убедитесь, что структура соответствует ожидаемой

### 3. Первый запуск моделей

При первом запуске модели будут автоматически загружены из Hugging Face. Убедитесь в наличии:
- Достаточного места на диске (модели занимают ~6-10 GB)
- Стабильного интернет-соединения

---

## Запуск сервиса

### Вариант 1: Разработка (Development)

#### Терминал 1 - Бэкенд:

```bash
# Активируйте виртуальное окружение
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows

# Перейдите в директорию проекта
cd autoexam-app

# Запуск сервера FastAPI
uvicorn server:app --host 0.0.0.0 --port 8000 --reload
```

Сервер будет доступен на: `http://localhost:8000`
API документация: `http://localhost:8000/docs`

#### Терминал 2 - Фронтенд:

```bash
cd autoexam-app

# Запуск Vite dev-сервера
npm run dev
```

Фронтенд будет доступен на: `http://localhost:5173`

### Вариант 2: Использование скриптов (рекомендуется)

**Linux/macOS - создайте `start_backend.sh`:**
```bash
#!/bin/bash
cd "$(dirname "$0")"
source venv/bin/activate
uvicorn server:app --host 0.0.0.0 --port 8000 --reload
```

**Windows - создайте `start_backend.bat`:**
```batch
@echo off
cd /d %~dp0
call venv\Scripts\activate
uvicorn server:app --host 0.0.0.0 --port 8000 --reload
pause
```

**Для фронтенда - `start_frontend.sh` (Linux/macOS):**
```bash
#!/bin/bash
cd "$(dirname "$0")"
npm run dev
```

**Или `start_frontend.bat` (Windows):**
```batch
@echo off
cd /d %~dp0
npm run dev
pause
```

### Проверка работы сервиса

1. Откройте браузер: `http://localhost:5173`
2. Загрузите тестовый CSV файл
3. Проверьте логи бэкенда на наличие ошибок

---

## Развертывание в продакшене

### 1. Сборка фронтенда

```bash
cd autoexam-app
npm run build
```

Собранные файлы будут в папке `dist/`

### 2. Настройка продакшен-сервера

#### Вариант A: Использование Nginx + Uvicorn

**Установка Nginx:**
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install nginx

# CentOS/RHEL
sudo yum install nginx
```

**Конфигурация Nginx (`/etc/nginx/sites-available/autoexam`):**
```nginx
server {
    listen 80;
    server_name your-domain.com;

    # Фронтенд (статические файлы)
    location / {
        root /path/to/autoexam-app/dist;
        try_files $uri $uri/ /index.html;
        index index.html;
    }

    # Проксирование API запросов к бэкенду
    location /api {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Увеличенные таймауты для длительных операций
        proxy_read_timeout 86400s;
        proxy_connect_timeout 86400s;
        proxy_send_timeout 86400s;
    }
}
```

**Активация конфигурации:**
```bash
sudo ln -s /etc/nginx/sites-available/autoexam /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### Вариант B: Использование systemd для бэкенда

**Создайте `/etc/systemd/system/autoexam-backend.service`:**
```ini
[Unit]
Description=AutoExam Backend Service
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/autoexam-app
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/uvicorn server:app --host 0.0.0.0 --port 8000 --workers 2
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Запуск сервиса:**
```bash
sudo systemctl daemon-reload
sudo systemctl enable autoexam-backend
sudo systemctl start autoexam-backend
sudo systemctl status autoexam-backend
```

### 3. Использование Gunicorn (альтернатива Uvicorn)

```bash
pip install gunicorn
gunicorn server:app -w 2 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:8000 --timeout 86400
```

### 4. Настройка SSL/HTTPS

**Установка Certbot:**
```bash
sudo apt install certbot python3-certbot-nginx
```

**Получение сертификата:**
```bash
sudo certbot --nginx -d your-domain.com
```

Автоматическое обновление настроено по умолчанию.

### 5. Настройка файрвола

```bash
# Ubuntu/Debian
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

# CentOS/RHEL
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

---

## Мониторинг и логирование

### 1. Логи бэкенда

Логи выводятся в консоль с уровнем INFO:
```
[inference] ========== ЗАПУСК ИНФЕРЕНСА: 51 строк ==========
[inference] Шаг 1/5: Нормализация данных
[server] GET /api/results/result-xxx - запрос статуса
```

**Для сохранения логов в файл:**

Измените в `server.py`:
```python
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('autoexam.log'),
        logging.StreamHandler()
    ]
)
```

### 2. Мониторинг ресурсов

**Проверка использования GPU:**
```bash
watch -n 1 nvidia-smi
```

**Проверка памяти:**
```bash
free -h  # Linux
```

**Мониторинг процессов:**
```bash
htop  # или top
```

### 3. Логи Nginx

```bash
# Access logs
sudo tail -f /var/log/nginx/access.log

# Error logs
sudo tail -f /var/log/nginx/error.log
```

---

## Решение проблем

### Проблема: Модели не загружаются

**Решение:**
1. Проверьте доступ к Hugging Face
2. Убедитесь в наличии места на диске
3. Проверьте логи на наличие ошибок загрузки

```bash
# Принудительное обновление кеша
export HF_HOME=/path/to/models
```

### Проблема: Out of Memory (OOM)

**Решение:**
1. Уменьшите batch size в `inference.py`
2. Используйте `bitsandbytes` для квантизации
3. Увеличьте объем VRAM или используйте CPU

### Проблема: Порт занят

**Решение:**
```bash
# Поиск процесса на порту
lsof -i :8000  # Linux/macOS
netstat -ano | findstr :8000  # Windows

# Изменение порта в server.py или uvicorn команде
uvicorn server:app --port 8001
```

### Проблема: CORS ошибки

**Решение:**
Убедитесь, что в `server.py` правильно настроен CORS:
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # Для продакшена укажите конкретные домены
    allow_credentials=False,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### Проблема: Файлы не скачиваются

**Решение:**
1. Проверьте права доступа к папке `storage/`
2. Убедитесь, что файлы создаются в `storage/results/`
3. Проверьте логи бэкенда на наличие ошибок

```bash
# Установка прав
chmod -R 755 storage/
```

### Проблема: Таймауты при обработке

**Решение:**
Система настроена на ожидание до 24 часов. Если проблемы продолжаются:
1. Проверьте логи бэкенда
2. Убедитесь, что обработка не падает с ошибкой
3. Проверьте использование памяти GPU

---

## Безопасность

### 1. Переменные окружения

Создайте файл `.env`:
```env
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=your-domain.com
DEBUG=False
```

### 2. Ограничение доступа

В продакшене:
- Используйте конкретные домены в CORS вместо `*`
- Настройте файрвол для ограничения доступа
- Используйте HTTPS

### 3. Резервное копирование

Регулярно создавайте бэкапы:
- Папка `data/` (история)
- Папка `storage/` (результаты)
- Конфигурационные файлы

```bash
# Пример скрипта бэкапа
tar -czf backup-$(date +%Y%m%d).tar.gz data/ storage/
```

---

## Оптимизация производительности

### 1. Использование GPU

Убедитесь, что CUDA доступна:
```python
import torch
print(torch.cuda.is_available())
print(torch.cuda.get_device_name(0))
```

### 2. Оптимизация памяти

Проект уже включает автоматическую очистку памяти CUDA после каждого процесса. Если нужна дополнительная оптимизация:
- Используйте квантизацию моделей
- Уменьшите размер батчей
- Используйте gradient checkpointing

### 3. Кэширование

Для ускорения повторных запросов можно добавить кэширование результатов в Redis или Memcached.

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи (`autoexam.log` или консоль)
2. Проверьте документацию по ошибкам выше
3. Создайте issue в репозитории с описанием проблемы

---

## Чеклист развертывания

- [ ] Установлены все системные зависимости
- [ ] Создано виртуальное окружение Python
- [ ] Установлен PyTorch с CUDA поддержкой
- [ ] Установлены все Python зависимости
- [ ] Установлены все Node.js зависимости
- [ ] Проверена работа моделей
- [ ] Собран фронтенд для продакшена
- [ ] Настроен веб-сервер (Nginx)
- [ ] Настроен systemd сервис для бэкенда
- [ ] Настроен SSL/HTTPS
- [ ] Настроен файрвол
- [ ] Настроен мониторинг и логирование
- [ ] Протестирована загрузка и обработка файлов
- [ ] Проверена работа скачивания результатов

---

